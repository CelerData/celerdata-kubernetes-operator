# This file is generated — do not edit it by hand.
apiVersion: v1
kind: Namespace
metadata:
  name: phoenixai
---
# Source: operator/templates/service_account.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: phoenixai
  namespace: phoenixai
---
# Source: operator/templates/clusterrole.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kube-anywhere-operator
  labels:
    app: kube-anywhere-operator
rules:
- apiGroups:
  - apps
  resources:
  - deployments
  - statefulsets
  verbs:
  - '*'
- apiGroups:
  - autoscaling
  resources:
  - horizontalpodautoscalers
  verbs:
  - '*'
- apiGroups:
  - batch
  resources:
  - cronjobs
  verbs:
  - '*'
- apiGroups:
  - policy
  resources:
  - poddisruptionbudgets
  verbs:
  - '*'
- apiGroups:
  - ""
  resources:
  - configmaps
  - serviceaccounts
  - services
  verbs:
  - '*'
- apiGroups:
  - ""
  resources:
  - endpoints
  - pods
  - secrets
  verbs:
  - get
  - list
  - watch
# PVC read is granted unconditionally so the query API can list PVCs for the resource tree.
# PVC write (create/update/patch) stays gated behind enablePVCExpansion; see rbac-pvc-expansion.yaml.
- apiGroups:
  - ""
  resources:
  - persistentvolumeclaims
  verbs:
  - get
  - list
  - watch
# Record Warning events (e.g. SyncFeFailed, PVC validation rejections) on the CR.
- apiGroups:
  - ""
  resources:
  - events
  verbs:
  - create
  - patch
- apiGroups:
  - phoenixdata.ai
  resources:
  - phoenixaiclusters
  - phoenixaiwarehouses
  verbs:
  - '*'
# when deploying to OpenShift, the following permission is also necessary
- apiGroups:
  - phoenixdata.ai
  resources:
  - phoenixaiclusters/finalizers
  - phoenixaiwarehouses/finalizers
  verbs:
  - update
- apiGroups:
  - phoenixdata.ai
  resources:
  - phoenixaiclusters/status
  - phoenixaiwarehouses/status
  verbs:
  - get
  - patch
  - update
---
# Source: operator/templates/rbac-pvc-expansion.yaml
# RBAC for the PVC volume expansion feature. Rendered when the feature is enabled
# (enablePVCExpansion) OR when only its RBAC is pre-provisioned (enablePVCExpansionRBAC). The latter
# lets deploy/operator.yaml ship these permissions with the feature still OFF, so an operator can be
# flipped on later (just add --enable-pvc-expansion) without re-applying cluster-scoped RBAC.
# CAVEAT: since the gRPC-query-API release moved PVC READ into the main operator RBAC, this file is
# no longer self-contained — pre-provisioning it standalone only works when the main operator RBAC
# is from that release or newer; against an older main RBAC the operator would get PVC write without
# read and fail Forbidden on every expansion reconcile.
#
# persistentvolumeclaims is namespaced; storageclasses is cluster-scoped (always needs a ClusterRole).
# PVC get/list/watch is granted unconditionally in the main operator RBAC (clusterrole.yaml, or
# role.yaml in watchNamespace mode — clusterrole.yaml renders nothing there), so this file only
# needs to add the write verbs the expansion feature requires.
# Cluster-wide mode: both resources granted via a single ClusterRole.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kube-anywhere-operator-pvc-expansion
  labels:
    app: kube-anywhere-operator
rules:
- apiGroups:
  - ""
  resources:
  - persistentvolumeclaims
  verbs:
  - create
  - update
  - patch
- apiGroups:
  - storage.k8s.io
  resources:
  - storageclasses
  verbs:
  - get
  - list
  - watch
---
# Source: operator/templates/clusterrolebinding.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kube-anywhere-operator
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kube-anywhere-operator
subjects:
- kind: ServiceAccount
  name: phoenixai
  namespace: phoenixai
---
# Source: operator/templates/rbac-pvc-expansion.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kube-anywhere-operator-pvc-expansion
  labels:
    app: kube-anywhere-operator
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kube-anywhere-operator-pvc-expansion
subjects:
- kind: ServiceAccount
  name: phoenixai
  namespace: phoenixai
---
# Source: operator/templates/leader-election-role.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: phoenixai-leader-election-role
  namespace: phoenixai
rules:
- apiGroups:
  - ""
  resources:
  - configmaps
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - coordination.k8s.io
  resources:
  - leases
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - ""
  resources:
  - events
  verbs:
  - create
  - patch
---
# Source: operator/templates/leader-election-role-binding.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: phoenixai-leader-election-rolebinding
  namespace: phoenixai
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: phoenixai-leader-election-role
subjects:
- kind: ServiceAccount
  name: phoenixai
  namespace: phoenixai
---
# Source: operator/templates/api-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: kube-anywhere-operator-api
  namespace: phoenixai
  labels:
    app: kube-anywhere-operator
spec:
  type: ClusterIP
  selector:
    app: kube-anywhere-operator
  ports:
  - port: 9090
    targetPort: grpc-api
    protocol: TCP
    name: grpc-api
---
# Source: operator/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kube-anywhere-operator
  namespace: phoenixai
  labels:
    app: kube-anywhere-operator
spec:
  selector:
    matchLabels:
      app: kube-anywhere-operator
  replicas: 1
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: manager
      labels:
        app: kube-anywhere-operator
        version: 2.0.0
    spec:
      automountServiceAccountToken: true
      containers:
      - command:
        - /phoenixai-operator
        args:
        - --leader-elect
        - --zap-time-encoding=iso8601
        - --zap-encoder=console
        - --volume-name-with-hash=true
        - --enable-pvc-expansion
        - --enable-pod-disruption-budget
        - --enable-api-server
        - --api-server-bind-address=:9090
        - --fe-ssl-mode=PREFERRED
        env:
        - name: TZ
          value: UTC
        image: "us-west1-docker.pkg.dev/phoenix-ai-images/enterprise/operator:v2.0.0"
        imagePullPolicy: Always
        name: manager
        ports:
        - containerPort: 9090
          name: grpc-api
          protocol: TCP
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        # More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
        resources:
          limits:
            cpu: 500m
            memory: 800Mi
          requests:
            cpu: 500m
            memory: 400Mi
      serviceAccountName: phoenixai
      terminationGracePeriodSeconds: 10
